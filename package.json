{
  "name": "website-reactjs-tutorial",
  "version": "1.0.0",
  "description": "Das ist eine Einführung in React!",
  "main": "src/app.js",
  "scripts": {
    "start": "npx servor ./public --reload",
    "compile:once": "babel src --out-dir public/lib",
    "compile": "babel src --out-dir public/lib --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonas Strube",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.16.0",
    "@babel/core": "^7.16.0",
    "@babel/preset-react": "^7.16.0"
  }
}
